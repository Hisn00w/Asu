# 贡献 ASu Agent

这份指南旨在节省双方时间。

## 理念

首先：**ASu Agent 的核心是极简的**。

如果你的功能不属于核心，那它应该是一个扩展。会让核心膨胀的 PR 很可能会被拒绝。

ASu Agent 的核心之所以存在，是为了保持极简和可扩展，从而可以被扩展影响和改造。但即使是扩展的 hook 点，也需要经过充分考量和讨论，以避免引入无法维护的膨胀和复杂交互。

## 唯一规则

**你必须理解自己的代码。** 如果你无法解释你的改动做了什么、如何与系统其他部分交互，你的 PR 会被关闭。

使用 AI 写代码没问题。但提交不经理解的 AI 生成代码是不允许的。

如果你使用 agent，请从 ASu Agent 根目录运行它，这样它会自动读取 `AGENTS.md`。你的 agent 必须遵守该文件中的规则与指南。

## 贡献门槛

新贡献者的所有 issue 和 PR 默认会被自动关闭。


## Issue 质量门槛

如果你要开 issue，必须使用两个 GitHub issue 模板之一。

Issue 要简短、具体、值得阅读。

- 保持简洁。如果一屏装不下，就太长了。
- 用你自己的声音写（不要用 LLM 生成文本；如果必须，后续补一条明确标注为 AI 的评论）。
- 清楚说明 bug 或需求。
- 解释它为什么重要。
- 如果你想自己实现这个改动，请说明。

如果 issue 真实且写得好，维护者可能会重新打开它，或在上述指定位置回复 `lgtmi` 或 `lgtm`。

## 封禁

如果你两次无视这份文档，或者用 agent 生成大量垃圾 issue，你的 GitHub 账号将被永久封禁。

如果你通过自动化手段发送大量 issue，你的 GitHub 账号将被永久封禁。No taksies backsies。

## 提交 PR 前

除非维护者已按上述规则用 `lgtm` 批准过你，否则不要开 PR。

提交 PR 前：

```bash
npm run check
./test.sh
```

两者都必须通过。

不要编辑 `CHANGELOG.md`。Changelog 条目由维护者添加。

如果你要向 `packages/ai` 添加新 provider，参见 `AGENTS.md` 中的必需测试。
